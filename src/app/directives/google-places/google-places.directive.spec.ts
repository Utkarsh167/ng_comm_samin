import { GooglePlacesDirective } from './google-places.directive';

describe('GooglePlacesDirective', () => {
  it('should create an instance', () => {
    const directive = ngnew GooglePlacesDirective();
    expect(directive).toBeTruthy();
  });
});
